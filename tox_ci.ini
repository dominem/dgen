[tox]
envlist=py36

[testenv]
passenv=CIRCLECI CIRCLE_* CI_PULL_REQUEST COVERALLS_REPO_TOKEN
deps=
    pytest
    pytest-cov
    Jinja2
    coveralls
commands=
    py.test -vv --cov dgen {posargs}
    coveralls
